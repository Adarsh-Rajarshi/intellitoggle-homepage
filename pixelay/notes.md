# Pixelay Overlay Notes

## Desktop Overlay (pixelay-desktop.png)

- Hero section headline font weight appears slightly lighter than Figma — exact font not available as a Google Font, used system bold instead
- Dashboard mockup is coded (no exported PNG from Figma) so layout matches but screenshot textures differ
- Spacing on pricing cards is within 2-4px of design

## Mobile Overlay (pixelay-mobile.png)

- Navigation collapses correctly into hamburger menu at mobile breakpoint
- CTA buttons stack vertically on small screens matching mobile Figma frame
- Footer columns reflow to single column on mobile as per design

## General Notes

- Primary colors (#3B39C7 purple, #F5A623 orange) matched exactly from Figma
- Font is closest available match — Figma uses a custom font not available via CDN